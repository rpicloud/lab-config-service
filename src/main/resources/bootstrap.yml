spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rpicloud/lab-config.git
server:
  port: 8888
---
spring:
  profiles: development
server:
  port: 8888
